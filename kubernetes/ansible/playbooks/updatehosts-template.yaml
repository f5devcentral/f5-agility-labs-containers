- hosts: kubernetes
  gather_facts: False
  tasks:
  - name: update /etc/hosts file
    become: true
    blockinfile:
      dest: /etc/hosts
      content: "{{ lookup('template', '../templates/hosts.j2') }}"
      state: present
